node_modules/
dist/
.vite/
.build/
__pycache__/
dist-pak/
public/wasm/*.js
public/wasm/*.wasm
public/wasm/*.data
public/wasm/base/
*.pk4
.DS_Store

# WebGPU port build artifacts: committed for now so GitHub Pages serves
# them (iOS Safari only exposes navigator.gpu on secure contexts, so LAN
# dev server can't test). Add CI build step + re-ignore later.
#public/wasm-webgpu/triangle.js
#public/wasm-webgpu/triangle.wasm
areadump-out/

# Area-streaming (Phase 4) artifacts — DEPLOYED 2026-06-14 so ?areastream works
# live (smallest boot gate). Regenerate with scripts/bake-area-stream.sh after a
# bproc/cache format bump or to retune the boot region, then re-commit.
# (Previously gitignored as experimental.)
.gstack/

# Display paks (reduced DOOM 3 game data, ~113MB) — NOT shipped in the repo.
# Host them yourself (e.g. Cloudflare R2) and pass the base URL via ?pak=<url>.
# The engine .js/.wasm/.data stay on the GitHub Pages origin.
public/wasm/base/
public/wasm/base-stream/
public/wasm/base256/

# R-GLTF spike test fixtures (regenerable from content-forge)
public/wasm/*.glb
