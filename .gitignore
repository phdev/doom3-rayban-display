node_modules/
dist/
.vite/
.build/
__pycache__/
dist-pak/
public/wasm/*.js
public/wasm/*.wasm
public/wasm/*.data
public/wasm/base/
*.pk4
.DS_Store

# WebGPU port build artifacts: committed for now so GitHub Pages serves
# them (iOS Safari only exposes navigator.gpu on secure contexts, so LAN
# dev server can't test). Add CI build step + re-ignore later.
#public/wasm-webgpu/triangle.js
#public/wasm-webgpu/triangle.wasm
areadump-out/

# Area-streaming (Phase 4) build artifacts — experimental ?areastream only,
# regenerate with scripts/bake-area-stream.sh (commit them only to deploy the
# experiment live). The monolithic boot pak (pak-display.pk4.*) stays tracked.
public/wasm/base-stream/pak-display-stream.pk4*
public/wasm/base-stream/enpro.areas.*
.gstack/
