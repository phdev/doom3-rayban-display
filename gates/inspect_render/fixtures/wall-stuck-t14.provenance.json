{
  "arcoCommit": "3502b7ad9b4a9154e2fd63e83ff6d724926f6519",
  "contractV": 1,
  "tick": 14,
  "date": "2026-07-08",
  "genCmd": {
    "state": "node --import tsx tools/inspect/inspect.mjs dump tools/inspect/fixtures/wall-stuck.json@14 --full --json",
    "world": "node -e \"import('./tools/replay/replay-load.mjs').then(m=>process.stdout.write(JSON.stringify(m.replayLoad('tools/inspect/fixtures/wall-stuck.json').world)))\"",
    "compose": "scene = { v: 1, world, state } — world and state embedded verbatim from the two commands above"
  },
  "note": "Generated from a DETACHED Arco worktree checked out at the exact pinned SHA 3502b7a (git worktree add --detach), NOT from Arco HEAD. At generation time Arco HEAD had drifted to cfb35c3, but the drift pinned->HEAD is 5 coordination/ ledger files ONLY: the ENTIRE non-coordination code surface (tools/inspect, tools/replay, docs/CORE_HEADLESS_EXPORTS.md, and the .ts integrator transitively imported by step-to-tick) is byte-identical between the pin and HEAD (verified: `git diff --name-only 3502b7a..HEAD -- ':!coordination'` is empty). So this fixture is the literal fulfillment of 'Pinned source: Arco @ 3502b7a' and is reproducible from either state. Flagged for owner ratification in REPORT BACK per TASK step 4."
}
